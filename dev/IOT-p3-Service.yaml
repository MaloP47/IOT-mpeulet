apiVersion: v1
kind: Service
metadata:
  name: wil-playground
  namespace: dev
  labels:
    app: svc-wil-playground
spec:
  selector:
    app: wil-playground
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: LoadBalancer
